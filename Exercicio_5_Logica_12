programa {
  funcao inicio() {
    inteiro valor = 123
    inteiro unidade, dezena, centena

    centena = valor / 100
    dezena = (valor % 100) / 10
    unidade = valor % 10

    escreva(unidade, dezena, centena) 

    
  }
}
