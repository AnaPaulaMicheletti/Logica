programa {
  funcao inicio() {
    inteiro valor 
    inteiro unidade, dezena, centena
    escreva("escolha um numero de 3 algarismos: ")
    leia(valor)

    centena = valor / 100
    dezena = (valor % 100) / 10
    unidade = valor % 10

    escreva(unidade, dezena, centena) 

    
  }
}
